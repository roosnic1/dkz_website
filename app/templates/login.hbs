<div class="row">
  <div class="col-md-12">

    {{#if session.isAuthenticated}}
      <a {{action 'logout'}}>Logout</a>
      <a {{action 'printAuth'}}>Print</a>
      {{#link-to 'admin'}}ADMIN{{/link-to}}
    {{else}}
      <a {{action 'sessionRequiresAuthentication'}}>Login</a>
    {{/if}}

    Session: {{ session.isAuthenticated }}
  </div>
</div>
